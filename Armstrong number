#include<stdio.h>
int main()
{
int n,d=0,sum-0,x,y,r;
scanf("%d",&n);
while(n>0)
{
d++;
n=n/10;
}
while(x>0)
{
r=x%10;
sum=sum+pow(r,d);
x=x/10;
}
if(y==sum)
printf("Armstrong number");
else
printf("Not an Armstrong Number");
return 0;
}
